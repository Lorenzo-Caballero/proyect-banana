<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Para sacarle una foto al comprobante desde el chat. Opcional: si el
         telefono no tiene camara la app funciona igual (solo galeria). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Avisos de bonos, fichas de regalo y promos. Desde Android 13 hay que
         pedirlo en runtime; en 12 y anteriores alcanza con declararlo. El
         widget lo pide sobre un gesto del jugador, no de arranque. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- PEDIR QUEDAR FUERA DE LA OPTIMIZACION DE BATERIA.
         Sin esto el sistema mata el trabajo periodico de WorkManager y el
         sondeo con la app cerrada no corre. Medido el 19/09/2026 sobre los 42
         celulares con permiso: sondearon en 24 horas 5 de 6 Samsung, 2 de 13
         Motorola y 1 de 20 Xiaomi. No es un bug del worker -- es el fabricante
         apagandolo.

         Google Play restringe este permiso a apps que lo justifiquen. No
         aplica: este APK se baja de nuestra propia pagina y nunca estuvo en
         Play Store. Si algun dia se publica ahi, hay que cambiarlo por
         ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS (abre la lista general en
         vez de pedir directo: peor experiencia, pero sin permiso especial). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:usesCleartextTraffic="false"
        android:hardwareAccelerated="true"
        android:theme="@style/Tema.Goldpaw">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="fullSensor"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Necesario para pasarle al selector de camara un archivo propio -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Para poder abrir links externos (banco, WhatsApp) en su propia app -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

</manifest>
